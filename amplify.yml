version: 1
frontend:
  phases:
    build:
      commands:
        - echo "Static site — no build step"
  artifacts:
    baseDirectory: /
    files:
      - index.html
      - cb-app.html
      - apexsensations.html
      - signup.html
  cache:
    paths: []
